import React from 'react';

const Header = () => {
    return (
        <header style={{ padding: '1rem', background: '#333', color: '#fff' }}>
            <h1>Digital Printing Shop Management</h1>
        </header>
    );
};

export default Header;
